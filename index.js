class SchemaBuilder {
    constructor() {

    }
    build(...args) {

    }
}

/*
import {SchemaBuidler, StrictString} from strict-schema"

prefaced by "Strict"
* required
* min
* max
* matches
* 

const schema = SchemaBuilder.build({
    fullName: StrictString('Name Must Be String.').StrictRequired('Name Is Required.'),
})

*/